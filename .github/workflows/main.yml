name: Portfolio ci
on: workflow_dispath
jobs:
  Testing:
    runs-on: ubuntu-latest
    steps:
    - name: Code checkout
      uses: actions/checkout@v4
      
    - name: npm test
      run: npm test
